# Changelog

The canonical changelog lives at the repository root: [CHANGELOG.md](../CHANGELOG.md).

This file previously carried a duplicate copy, which drifted from the root one. It is now a
pointer so there is exactly one history to trust.
