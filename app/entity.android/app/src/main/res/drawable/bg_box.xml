<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="?attr/monoSurface" />
    <!-- 1dp of mono_outline, not 2dp of mono_fg: a full-strength 2dp border around every
         card is a lot of bright line area, and area is what makes a dark UI glare. -->
    <stroke android:width="1dp" android:color="?attr/monoOutline" />
    <corners android:radius="@dimen/mono_radius" />
</shape>
