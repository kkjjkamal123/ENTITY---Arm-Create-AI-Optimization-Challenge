<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The only network use is downloading a model you pick from the catalog.
         Inference never touches the network, and no result ever leaves the phone. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BenchApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EntityBench">

        <activity
            android:name=".BenchHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Portrait-locked: a rotation would tear down the in-flight benchmark. -->
        <activity
            android:name=".RunActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:parentActivityName=".BenchHomeActivity" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:parentActivityName=".BenchHomeActivity" />
        <activity
            android:name=".ContributeActivity"
            android:exported="false" />


        <activity
            android:name=".ModelsActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".BenchHomeActivity" />
    </application>

</manifest>
