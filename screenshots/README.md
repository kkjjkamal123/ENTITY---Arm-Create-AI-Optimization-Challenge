# Screenshots

Split by app. Filenames ending in `N` are the dark theme.

| Folder | App |
|---|---|
| [`Entity Chat/`](Entity%20Chat/) | ENTITY, the chat app |
| [`Entity Bench/`](Entity%20Bench/) | ENTITY Bench, the measurement app |

Markdown links to these paths need `%20` for the space in the folder names.

Captured on a CMF Phone 1 (Dimensity 7300, Android 16) unless a caption says otherwise. They are
illustrative of the interface, not evidence - measurements live in
[`../benchmarks/`](../benchmarks/).
